// Single source of truth for the site footer: every page (home and the rest)
// uses the same footer component so they never drift apart. The full
// implementation lives in components/lovable/Footer.tsx.
export { default } from '@/components/lovable/Footer';
